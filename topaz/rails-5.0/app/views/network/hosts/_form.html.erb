<%= form_for(network_host) do |f| %>
  <% if network_host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(network_host.errors.count, "error") %> prohibited this network_host from being saved:</h2>

      <ul>
      <% network_host.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :host_name %>
    <%= f.text_field :host_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
