<%= form_for(network_header) do |f| %>
  <% if network_header.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(network_header.errors.count, "error") %> prohibited this network_header from being saved:</h2>

      <ul>
      <% network_header.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :header_name %>
    <%= f.text_field :header_name %>
  </div>

  <div class="field">
    <%= f.label :header_value %>
    <%= f.text_area :header_value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
