<%= form_for(network_domain) do |f| %>
  <% if network_domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(network_domain.errors.count, "error") %> prohibited this network_domain from being saved:</h2>

      <ul>
      <% network_domain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
