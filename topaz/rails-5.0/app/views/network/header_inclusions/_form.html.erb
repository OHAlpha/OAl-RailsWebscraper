<%= form_for(network_header_inclusion) do |f| %>
  <% if network_header_inclusion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(network_header_inclusion.errors.count, "error") %> prohibited this network_header_inclusion from being saved:</h2>

      <ul>
      <% network_header_inclusion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :set_id %>
    <%= f.number_field :set_id %>
  </div>

  <div class="field">
    <%= f.label :header_id %>
    <%= f.number_field :header_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
