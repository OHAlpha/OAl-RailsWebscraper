<%= form_for(network_header_set) do |f| %>
  <% if network_header_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(network_header_set.errors.count, "error") %> prohibited this network_header_set from being saved:</h2>

      <ul>
      <% network_header_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :headers_string %>
    <%= f.text_area :headers_string %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
