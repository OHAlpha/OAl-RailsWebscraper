<%= form_for(content_html_link) do |f| %>
  <% if content_html_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(content_html_link.errors.count, "error") %> prohibited this content_html_link from being saved:</h2>

      <ul>
      <% content_html_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :source_id %>
    <%= f.number_field :source_id %>
  </div>

  <div class="field">
    <%= f.label :target_id %>
    <%= f.number_field :target_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
