<%= form_for(content_document) do |f| %>
  <% if content_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(content_document.errors.count, "error") %> prohibited this content_document from being saved:</h2>

      <ul>
      <% content_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :avenue_id %>
    <%= f.number_field :avenue_id %>
  </div>

  <div class="field">
    <%= f.label :source_id %>
    <%= f.number_field :source_id %>
  </div>

  <div class="field">
    <%= f.label :contents_count %>
    <%= f.number_field :contents_count %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
